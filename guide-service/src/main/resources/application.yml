server:
  port: 8104

spring:
  application:
    name: guide-service
  cloud:
    nacos:
      username: nacos
      password: yaolezhang1119
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: ${REDIS_GUIDE_DB:1}

dashscope:
  api-key: ${DASHSCOPE_API_KEY:sk-b2380d5cbf064b4f9ea2546127bd816f}
  model: ${DASHSCOPE_MODEL:qwen-plus}

app:
  guide:
    task-ttl-hours: 2

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 120000
